# This file automatically generated by skymarshal
# DO NOT MODIFY BY HAND
# fmt: off
# isort: off
# mypy: disallow-untyped-defs
#
# From Source File: /project/third_party/eigen_lcm/lcmtypes/eigen_lcm.lcm

import copy
import typing as T  # pylint: disable=unused-import

from io import BytesIO
import struct

class VectorXf(object):
    __slots__: T.List[str] = ["rows", "data"]

    def __init__(
        self,
        rows: int=0,
        data: T.Optional[T.Sequence[float]]=None,
        _skip_initialize: bool=False,
    ) -> None:
        """ If _skip_initialize is True, all other constructor arguments are ignored """
        if _skip_initialize:
            return
        self.rows: int = rows
        self.data: T.Sequence[float] = [] if data is None else data

    @staticmethod
    def from_all_fields(
        rows: int,
        data: T.Sequence[float],
    ) -> "VectorXf":
        return VectorXf(
            rows=rows,
            data=data,
        )

    @staticmethod
    def _skytype_meta() -> T.Dict[str, str]:
        return dict(
            type="struct",
            package="eigen_lcm",
            name="VectorXf",
        )

    @classmethod
    def _default(cls) -> "VectorXf":
        return cls()

    def __repr__(self) -> str:
        return "VectorXf({})".format(
            ", ".join("{}={}".format(name, repr(getattr(self, name))) for name in self.__slots__))

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, VectorXf):
            return NotImplemented
        return (
            (self.rows==other.rows) and
            (self.data==other.data)
        )
    # Disallow hashing for python struct lcmtypes.
    __hash__ = None  # type: ignore[assignment]

    def encode(self) -> bytes:
        buf = BytesIO()
        buf.write(VectorXf._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf: T.BinaryIO) -> None:
        buf.write(VectorXf._CACHED_STRUCT_0.pack(self.rows))
        buf.write(struct.pack('>%df' % self.rows, *self.data[:self.rows]))

    @staticmethod
    def decode(data: T.Union[bytes, T.BinaryIO]) -> "VectorXf":
        # NOTE(eric): This function can technically accept either a BinaryIO or
        # anything that supports the C++ Buffer Protocol,
        # which is unspecifiable in type hints.

        if hasattr(data, "read"):
            # NOTE(eric): mypy isn't able to figure out the hasattr check
            buf = T.cast(T.BinaryIO, data)
        else:
            buf = BytesIO(T.cast(bytes, data))

        if buf.read(8) != VectorXf._get_packed_fingerprint():
            raise ValueError("Decode error")
        return VectorXf._decode_one(buf)

    @staticmethod
    def _decode_one(buf: T.BinaryIO) -> "VectorXf":
        self = VectorXf(_skip_initialize=True)
        self.rows = VectorXf._CACHED_STRUCT_0.unpack(buf.read(4))[0]
        self.data = struct.unpack('>%df' % self.rows, buf.read(self.rows * 4))
        return self

    @staticmethod
    def _get_hash_recursive(parents: T.List[T.Type]) -> int:
        if VectorXf in parents: return 0
        tmphash = (0xd44af9becfc4d42a) & 0xffffffffffffffff
        tmphash = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash

    _packed_fingerprint: T.Optional[bytes] = None

    @staticmethod
    def _get_packed_fingerprint() -> bytes:
        if VectorXf._packed_fingerprint is None:
            VectorXf._packed_fingerprint = struct.pack(">Q", VectorXf._get_hash_recursive([]))
        return VectorXf._packed_fingerprint

    def deepcopy(self, **kwargs: T.Any) -> "VectorXf":
        """
        Deep copy of this LCM type

        Returns a copy w/ members specified by kwargs replaced with new values specified by kwargs.
        """
        result = copy.deepcopy(self)
        for key in kwargs:
            if not hasattr(result, key):
                raise KeyError("Type VectorXf does not have attribute: " + str(key))
            setattr(result, key, kwargs[key])
        return result

    _CACHED_STRUCT_0 = struct.Struct(">i")

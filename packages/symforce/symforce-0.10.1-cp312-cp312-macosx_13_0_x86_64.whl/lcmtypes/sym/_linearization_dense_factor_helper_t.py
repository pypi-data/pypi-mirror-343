# This file automatically generated by skymarshal
# DO NOT MODIFY BY HAND
# fmt: off
# isort: off
# mypy: disallow-untyped-defs
#
# From Source File: /Users/runner/work/symforce/symforce/lcmtypes/symforce.lcm

import copy
import typing as T  # pylint: disable=unused-import

from io import BytesIO
import struct
from lcmtypes.sym._linearization_dense_key_helper_t import linearization_dense_key_helper_t

class linearization_dense_factor_helper_t(object):
    __slots__: T.List[str] = ["residual_dim", "combined_residual_offset", "key_helpers"]

    def __init__(
        self,
        residual_dim: int=0,
        combined_residual_offset: int=0,
        key_helpers: T.Optional[T.List[linearization_dense_key_helper_t]]=None,
        _skip_initialize: bool=False,
    ) -> None:
        """ If _skip_initialize is True, all other constructor arguments are ignored """
        if _skip_initialize:
            return
        # Total residual dimension of the factor
        self.residual_dim: int = residual_dim
        # Offset of this factor's residual slice within the whole problem residual
        self.combined_residual_offset: int = combined_residual_offset
        self.key_helpers: T.List[linearization_dense_key_helper_t] = [] if key_helpers is None else key_helpers

    @staticmethod
    def from_all_fields(
        residual_dim: int,
        combined_residual_offset: int,
        key_helpers: T.List[linearization_dense_key_helper_t],
    ) -> "linearization_dense_factor_helper_t":
        return linearization_dense_factor_helper_t(
            residual_dim=residual_dim,
            combined_residual_offset=combined_residual_offset,
            key_helpers=key_helpers,
        )

    @staticmethod
    def _skytype_meta() -> T.Dict[str, str]:
        return dict(
            type="struct",
            package="sym",
            name="linearization_dense_factor_helper_t",
        )

    @classmethod
    def _default(cls) -> "linearization_dense_factor_helper_t":
        return cls()

    def __repr__(self) -> str:
        return "linearization_dense_factor_helper_t({})".format(
            ", ".join("{}={}".format(name, repr(getattr(self, name))) for name in self.__slots__))

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, linearization_dense_factor_helper_t):
            return NotImplemented
        return (
            (self.residual_dim==other.residual_dim) and
            (self.combined_residual_offset==other.combined_residual_offset) and
            (self.key_helpers==other.key_helpers)
        )
    # Disallow hashing for python struct lcmtypes.
    __hash__ = None  # type: ignore[assignment]

    def encode(self) -> bytes:
        buf = BytesIO()
        buf.write(linearization_dense_factor_helper_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf: T.BinaryIO) -> None:
        buf.write(linearization_dense_factor_helper_t._CACHED_STRUCT_0.pack(self.residual_dim, self.combined_residual_offset))
        v_num_key_helpers = len(self.key_helpers)
        buf.write(linearization_dense_factor_helper_t._CACHED_STRUCT_1.pack(v_num_key_helpers))
        for i0 in range(v_num_key_helpers):
            if hasattr(self.key_helpers[i0], '_get_packed_fingerprint'):
                assert self.key_helpers[i0]._get_packed_fingerprint() == linearization_dense_key_helper_t._get_packed_fingerprint()
            else:
                assert self.key_helpers[i0]._get_hash_recursive([]) == linearization_dense_key_helper_t._get_hash_recursive([])
            self.key_helpers[i0]._encode_one(buf)

    @staticmethod
    def decode(data: T.Union[bytes, T.BinaryIO]) -> "linearization_dense_factor_helper_t":
        # NOTE(eric): This function can technically accept either a BinaryIO or
        # anything that supports the C++ Buffer Protocol,
        # which is unspecifiable in type hints.

        if hasattr(data, "read"):
            # NOTE(eric): mypy isn't able to figure out the hasattr check
            buf = T.cast(T.BinaryIO, data)
        else:
            buf = BytesIO(T.cast(bytes, data))

        if buf.read(8) != linearization_dense_factor_helper_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return linearization_dense_factor_helper_t._decode_one(buf)

    @staticmethod
    def _decode_one(buf: T.BinaryIO) -> "linearization_dense_factor_helper_t":
        self = linearization_dense_factor_helper_t(_skip_initialize=True)
        self.residual_dim, self.combined_residual_offset = linearization_dense_factor_helper_t._CACHED_STRUCT_0.unpack(buf.read(8))
        v_num_key_helpers = linearization_dense_factor_helper_t._CACHED_STRUCT_1.unpack(buf.read(4))[0]
        self.key_helpers = []
        for i0 in range(v_num_key_helpers):
            self.key_helpers.append(linearization_dense_key_helper_t._decode_one(buf))
        return self

    @staticmethod
    def _get_hash_recursive(parents: T.List[T.Type]) -> int:
        if linearization_dense_factor_helper_t in parents: return 0
        newparents = parents + [linearization_dense_factor_helper_t]
        tmphash = (0x3c5d15dddc6d4e66+ linearization_dense_key_helper_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash

    _packed_fingerprint: T.Optional[bytes] = None

    @staticmethod
    def _get_packed_fingerprint() -> bytes:
        if linearization_dense_factor_helper_t._packed_fingerprint is None:
            linearization_dense_factor_helper_t._packed_fingerprint = struct.pack(">Q", linearization_dense_factor_helper_t._get_hash_recursive([]))
        return linearization_dense_factor_helper_t._packed_fingerprint

    def deepcopy(self, **kwargs: T.Any) -> "linearization_dense_factor_helper_t":
        """
        Deep copy of this LCM type

        Returns a copy w/ members specified by kwargs replaced with new values specified by kwargs.
        """
        result = copy.deepcopy(self)
        for key in kwargs:
            if not hasattr(result, key):
                raise KeyError("Type linearization_dense_factor_helper_t does not have attribute: " + str(key))
            setattr(result, key, kwargs[key])
        return result

    _CACHED_STRUCT_0 = struct.Struct(">ii")
    _CACHED_STRUCT_1 = struct.Struct(">i")

# This file automatically generated by skymarshal
# DO NOT MODIFY BY HAND
# fmt: off
# isort: off
# mypy: disallow-untyped-defs
#
# From Source File: /Users/runner/work/symforce/symforce/third_party/eigen_lcm/lcmtypes/eigen_lcm.lcm

import copy
import typing as T  # pylint: disable=unused-import

from io import BytesIO
import struct

class MatrixXi(object):
    __slots__: T.List[str] = ["rows", "data_sz", "data"]

    def __init__(
        self,
        rows: int=0,
        data_sz: int=0,
        data: T.Optional[T.Sequence[int]]=None,
        _skip_initialize: bool=False,
    ) -> None:
        """ If _skip_initialize is True, all other constructor arguments are ignored """
        if _skip_initialize:
            return
        self.rows: int = rows
        self.data_sz: int = data_sz
        self.data: T.Sequence[int] = [] if data is None else data

    @staticmethod
    def from_all_fields(
        rows: int,
        data_sz: int,
        data: T.Sequence[int],
    ) -> "MatrixXi":
        return MatrixXi(
            rows=rows,
            data_sz=data_sz,
            data=data,
        )

    @staticmethod
    def _skytype_meta() -> T.Dict[str, str]:
        return dict(
            type="struct",
            package="eigen_lcm",
            name="MatrixXi",
        )

    @classmethod
    def _default(cls) -> "MatrixXi":
        return cls()

    def __repr__(self) -> str:
        return "MatrixXi({})".format(
            ", ".join("{}={}".format(name, repr(getattr(self, name))) for name in self.__slots__))

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, MatrixXi):
            return NotImplemented
        return (
            (self.rows==other.rows) and
            (self.data_sz==other.data_sz) and
            (self.data==other.data)
        )
    # Disallow hashing for python struct lcmtypes.
    __hash__ = None  # type: ignore[assignment]

    def encode(self) -> bytes:
        buf = BytesIO()
        buf.write(MatrixXi._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf: T.BinaryIO) -> None:
        buf.write(MatrixXi._CACHED_STRUCT_0.pack(self.rows, self.data_sz))
        buf.write(struct.pack('>%di' % self.data_sz, *self.data[:self.data_sz]))

    @staticmethod
    def decode(data: T.Union[bytes, T.BinaryIO]) -> "MatrixXi":
        # NOTE(eric): This function can technically accept either a BinaryIO or
        # anything that supports the C++ Buffer Protocol,
        # which is unspecifiable in type hints.

        if hasattr(data, "read"):
            # NOTE(eric): mypy isn't able to figure out the hasattr check
            buf = T.cast(T.BinaryIO, data)
        else:
            buf = BytesIO(T.cast(bytes, data))

        if buf.read(8) != MatrixXi._get_packed_fingerprint():
            raise ValueError("Decode error")
        return MatrixXi._decode_one(buf)

    @staticmethod
    def _decode_one(buf: T.BinaryIO) -> "MatrixXi":
        self = MatrixXi(_skip_initialize=True)
        self.rows, self.data_sz = MatrixXi._CACHED_STRUCT_0.unpack(buf.read(8))
        self.data = struct.unpack('>%di' % self.data_sz, buf.read(self.data_sz * 4))
        return self

    @staticmethod
    def _get_hash_recursive(parents: T.List[T.Type]) -> int:
        if MatrixXi in parents: return 0
        tmphash = (0xa8245e97d27287f8) & 0xffffffffffffffff
        tmphash = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash

    _packed_fingerprint: T.Optional[bytes] = None

    @staticmethod
    def _get_packed_fingerprint() -> bytes:
        if MatrixXi._packed_fingerprint is None:
            MatrixXi._packed_fingerprint = struct.pack(">Q", MatrixXi._get_hash_recursive([]))
        return MatrixXi._packed_fingerprint

    def deepcopy(self, **kwargs: T.Any) -> "MatrixXi":
        """
        Deep copy of this LCM type

        Returns a copy w/ members specified by kwargs replaced with new values specified by kwargs.
        """
        result = copy.deepcopy(self)
        for key in kwargs:
            if not hasattr(result, key):
                raise KeyError("Type MatrixXi does not have attribute: " + str(key))
            setattr(result, key, kwargs[key])
        return result

    _CACHED_STRUCT_0 = struct.Struct(">ii")

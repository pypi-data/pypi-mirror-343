[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "repodex"
version = "0.1.0" # Initial version, can be updated as needed
description = "GitHub Repository Catalog: Fetching, indexing, and organizing READMEs."
readme = "README.md"
authors = [
    { name = "Shane Holloman", email = "shaneholloman@gmail.com" },
]
requires-python = ">=3.10"
license = { text = "MIT" } # Added license info based on README
urls = { Homepage = "https://github.com/shaneholloman/repodex" }
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
]
dependencies = [
    "packaging", # Used by release scripts
]

[project.scripts]
repodex = "repodex.cli:main"

[tool.ruff]
# Optional: Can move Ruff config from .clinerules here eventually,
# but leave it in .clinerules for now as per user instructions.
# line-length = 100
# target-version = "py310"
# select = ["E", "F", "B", "I", "UP", "N", "PL", "RUF"]
# ignore = ["E203"]

# [tool.ruff.per-file-ignores]
# "__init__.py" = ["F401"]

# [tool.ruff.flake8-quotes]
# docstring-quotes = "double"
# inline-quotes = "double"

# [tool.ruff.flake8-import-conventions.aliases]
# numpy = "np"
# pandas = "pd"

[tool.mypy]
# Optional: Configure mypy if needed, e.g., specify python version
# python_version = "3.10"

[tool.hatch.build.targets.wheel]
packages = ["repodex", "tools"]

[build-system]
requires = [
    "setuptools>=42",
    "setuptools_scm[toml]>=3.4.3",
    "wheel",
    "astropy>6.1"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "gwpopulation_pipe/_version.py"

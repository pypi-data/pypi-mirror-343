# VAssureAI Environment Variables

# --- Core Settings ---
# Base URL for the application under test
BASE_URL=https://www.example.com

# --- Credentials (Optional - Use secure methods if possible) ---
# Username for login (if applicable)
USERNAME=your_username
# Password for login (if applicable)
PASSWORD=your_password

# --- LLM Configuration ---
# Google Gemini API Key (Required for VAssureAI core functionality)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Specify a different Gemini model (Defaults to gemini-2.0-flash-exp)
# LLM_MODEL=gemini-pro

# Optional: Adjust LLM temperature (Defaults to 0.2)
# LLM_TEMPERATURE=0.5

# --- Framework Behavior (Optional Overrides) ---
# VASSURE_HEADLESS=true # Run browser in headless mode (true/false)
# VASSURE_RECORD_VIDEO=true # Record video of test execution (true/false)
# VASSURE_MAX_RETRIES=3 # Max retries for failed steps
# VASSURE_RETRY_DELAY=2 # Delay in seconds between retries
# VASSURE_HIGHLIGHT=true # Highlight elements during execution (true/false)
# VASSURE_SCREENSHOT_ON_STEP=true # Take screenshot on every step (true/false)
# VASSURE_SCREENSHOT_ON_ERROR=true # Take screenshot on error (true/false)

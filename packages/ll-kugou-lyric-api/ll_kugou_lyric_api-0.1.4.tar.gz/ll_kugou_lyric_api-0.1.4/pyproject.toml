[project]
name = "ll_kugou_lyric_api"
version = "0.1.4"
description = "A simple Kugou lyrics fetcher"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
  {name = "刘龙", email = "1053278842@qq.com"},
]
dependencies = [
  "requests",
  # 其他依赖写在这里
]

[build-system]
requires = ["flit_core>=3.2"]
build-backend = "flit_core.buildapi"

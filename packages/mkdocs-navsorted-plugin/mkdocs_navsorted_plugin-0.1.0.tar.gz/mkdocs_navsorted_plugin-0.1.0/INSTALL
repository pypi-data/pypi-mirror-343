mkdocs-navsorted-plugin installation
====================================


Python ``pip`` package is required to install ``mkdocs-navsorted-plugin``.


From sources
------------

Use the following command line to install ``mkdocs-navsorted-plugin`` from sources directory (containing setup.py):

    pip install .


From PyPI
---------

Alternatively you can install ``mkdocs-navsorted-plugin`` from PyPI:

    pip install mkdocs-navsorted-plugin


Use `-U` flag for upgrade:

    pip install -U mkdocs-navsorted-plugin


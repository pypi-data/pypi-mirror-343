[tool.poetry]
name = "random-gstin-generator"
version = "0.1.3"
description = "Creates random India GSTIN numbers that pass validation. For testing purposes only."
authors = ["Mohammed Saleel <mohammed.saleel0099@gmail.com>"]
readme = "README.md"
packages = [{ include = "gstin_generator", from = "src" }]

[tool.poetry.dependencies]
python = ">=3.8"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"
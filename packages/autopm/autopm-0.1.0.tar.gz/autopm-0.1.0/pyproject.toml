[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autopm"
version = "0.1.0"
description = "Auto-install and track Python packages during runtime"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Saptarshi Borgohain", email = "saptarshiborgohain6@gmail.com" }]
requires-python = ">=3.7"
dependencies = []

[project.scripts]
autopm = "autopm.cli:main"

[project]
name = "h3110w0r1d"
version = "0.1.2"
description = "A simple HelloWorld Python package."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
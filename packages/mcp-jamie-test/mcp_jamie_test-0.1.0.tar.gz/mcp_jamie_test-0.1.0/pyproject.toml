[project]
name = "mcp-jamie-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jamiezhen", email = "jamiezhen@tencent.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "openai>=1.76.0",
    "requests>=2.32.3",
]

[project.scripts]
mcp-jamie-test = "mcp_jamie_test.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "m-test-v10"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    { name = "lwmacct", email = "lwmacct@icloud.com" },
]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true

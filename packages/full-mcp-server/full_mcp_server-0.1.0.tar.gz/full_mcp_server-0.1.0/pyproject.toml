[project]
name = "full-mcp-server"
version = "0.1.0"
description = "Creating MCP project with theS SDK wizard"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [ "mcp>=1.6.0",]
[[project.authors]]
name = "Sakthivel Chellapparimanam"
email = "srcsak@amazon.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
full-mcp-server = "full_mcp_server:main"

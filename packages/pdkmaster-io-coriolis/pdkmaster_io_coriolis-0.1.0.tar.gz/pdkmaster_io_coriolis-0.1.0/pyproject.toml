[project]
name = "pdkmaster-io-coriolis"
dynamic = []
description = "PDKMaster integration with coriolis"
authors = [
    { name = "Staf Verhaegen", email = "staf@fibraservi.eu" },
]
dependencies = [
    "PDKMaster~=0.12.0",
]
requires-python = ">=3.8"
readme = "README.md"
version = "0.1.0"

[project.license]
text = "AGPL-3.0-or-later OR GPL-2.0-or-later OR CERN-OHL-S-2.0+ OR Apache-2.0"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[dependency-groups]
dev = [
    "doit>=0.36.0",
    "sphinx>=7.1.2",
]

[tool.pdm]
distribution = true

[tool.pdm.build]
includes = [
    "pdkmaster/",
]

[tool.pdm.version]
source = "scm"

[tool.pdm.scripts]
doit = "doit"
upd = "pdm update --update-reuse-installed"
updns = "pdm update --update-reuse-installed --no-sync"

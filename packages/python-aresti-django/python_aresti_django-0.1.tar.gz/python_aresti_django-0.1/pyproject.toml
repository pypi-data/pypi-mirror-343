[build-system]
requires = ["git-versiointi>=1.7b1", "setuptools"]
build-backend = "setuptools.build_meta"

[project]
authors = [
  {name = "Antti Hautaniemi", email = "antti.hautaniemi@pispalanit.fi"},
]
dependencies = ["Django", "python-aresti>=0.8"]
description = "Aresti-rajapintojen Django-testaustyökalut"
dynamic = ["version"]
license = {text = "MIT"}
name = "python-aresti-django"
readme = "README.md"
requires-python = ">= 3.8"
urls = {Repository = "https://github.com/an7oine/python-aresti-django.git"}

[tool.setuptools]
py-modules = ["aresti_django"]

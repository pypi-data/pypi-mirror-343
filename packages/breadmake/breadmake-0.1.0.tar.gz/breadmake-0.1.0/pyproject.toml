[build-system]
requires = ["setuptools>=79.0.1", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "breadmake"
version = "0.1.0"
description = "a simple build automation library and CLI tool"
authors = [
{ name="Bradley Hutchings", email="bkhnapa@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
	"Programming Language :: Python :: 3",
	"Development Status :: 2 - Pre-Alpha",
]

[project.scripts]
breadmake = "breadmake.breadmake_cli:main"

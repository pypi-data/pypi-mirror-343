[project]
name = "img2pdfify"
version = "0.2.5"
description = "Convert images to a single PDF via command line"
authors = [{ name = "Sharan Thakur" }]
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "Pillow"
]

[project.scripts]
img2pdfify = "img2pdfify.__main__:main"
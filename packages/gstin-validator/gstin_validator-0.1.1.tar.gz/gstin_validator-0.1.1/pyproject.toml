[project]
name = "gstin-validator"
version = "0.1.1"
description = "Validate India GSTIN numbers. Returns True if valid, False otherwise."
authors = [
    {name = "Mohammed Saleel", email = "mohammed.saleel0099@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"
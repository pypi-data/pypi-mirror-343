# This is the prefix for the branch on the backend where local sync changes are pushed to
# We use a random string to avoid conflicts with user-created branches
BACKEND_GIT_WORKING_BRANCH_NAME_PREFIX = "imbue_local_sync_878"

MAX_MESSAGE_STREAM_CHUNK_SIZE = 10 * 1024

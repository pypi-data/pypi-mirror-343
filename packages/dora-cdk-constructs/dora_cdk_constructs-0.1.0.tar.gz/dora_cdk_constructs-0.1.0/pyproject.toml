[tool.poetry]
name = "dora-cdk-constructs"
version = "0.1.0"
description = "Use this Dora CDK Construct Library to deploy applications using AWS CDK"
authors = ["Didone <tiago.didone@compass.uol>"]
license = "Apache-2.0"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
aws-cdk-lib = "^2.177.0"
constructs = "^10.4.2"
cdk-ecr-deployment = "^3.1.9"
dora-aws = "^0.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

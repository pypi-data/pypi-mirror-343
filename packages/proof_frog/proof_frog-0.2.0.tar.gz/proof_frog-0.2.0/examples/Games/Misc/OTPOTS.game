Game Left(Int lambda) {
    BitString<lambda> Eavesdrop(BitString<lambda> mL, BitString<lambda> mR) {
        BitString<lambda> k <- BitString<lambda>;
        return k + mL;
    }
}

Game Right(Int lambda) {
    BitString<lambda> Eavesdrop(BitString<lambda> mL, BitString<lambda> mR) {
        BitString<lambda> k <- BitString<lambda>;
        return k + mR;
    }
}

export as OTPOTS;

[project]
name = "ok-py-logging"
version = "0.1"
description = "Simple minimalist Python logging defaults"
readme = "README.md"
authors = [{ name = "Dan Egnor", email="egnor@ofb.net" }]
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://github.com/egnor/ok-py-logging#readme"
Repository = "https://github.com/egnor/ok-py-logging.git"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

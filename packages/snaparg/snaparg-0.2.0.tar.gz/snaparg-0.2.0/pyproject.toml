[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "snaparg"
version = "0.2.0"
description = "A typo-tolerant CLI parser wrapper for argparse"
authors = [{name="PJ Hayes", email="archood2next@gmail.com"}]
readme = "README.md"
requires-python = ">=3.6"
license = {text = "MPL 2.0"}

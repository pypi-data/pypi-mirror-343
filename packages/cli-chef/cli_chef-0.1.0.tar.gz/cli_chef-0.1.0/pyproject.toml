[tool.poetry]
name = "cli-chef"
version = "0.1.0"
description = "A CLI tool to bundle code files."      # Added a description
authors = ["Alex Thillen <alexthillen852@gmail.com>"]
readme = "README.md"
packages = [{ include = "cli_chef" }]

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.1.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
bundle = "cli_chef.bundle:main"

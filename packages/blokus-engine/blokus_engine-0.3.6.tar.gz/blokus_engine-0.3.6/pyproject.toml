[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "blokus_engine"
version = "0.3.6"
readme = "README.md"
requires-python = ">=3.7"
authors = [{ name = "Alex Racapé" }]
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
]

[tool.maturin]
manifest-path = "self_play/Cargo.toml"

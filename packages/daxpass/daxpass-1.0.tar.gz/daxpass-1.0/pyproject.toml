[project]
name = "daxpass"
version = "1.0"
description = "A module used to create random passwords with customizable options!"
authors = [{name="Annie"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"